name: Validation
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: dom, curl, libxml, mbstring, zip, pdo, gd
      - name: validate composer.json and composer.lock
        run: composer validate --strict
      - name: cache
        id: composer-cache
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-
      - name: composer install
        run: composer install --prefer-dist --no-progress
      - name: lint
        run: find . -name '*.php' ! -path './vendor/*' -exec php -l -n {} \; | (! grep -v "No syntax errors detected")
      - name: phpcs
        run: php vendor/bin/phpcs
      - name: run examples
        run: |
          for example in $(find primitives lists maps serialization -name '*.php' | sort); do
            echo "::group::$example"
            php -n "$example" || exit 1
            echo "::endgroup::"
          done
